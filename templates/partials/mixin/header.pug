mixin header(wrap)
    if wrap
        header.header-main.header-main--size
          include ../block/slogan.pug
    else
        header.header-main.header-main--pseudo
          .container.container--between
            include ../block/slogan.pug
            +result(true)